package com.example.game;

import android.view.View;
import android.widget.RelativeLayout;

public class BuildInfoView {
	private SceneData sd;
	RelativeLayout view_layout;
	public BuildInfoView(View view)
	{
		sd = SceneData.CreateData();
		view_layout = (RelativeLayout) view.findViewById(R.id.build_info);
        view_layout.setVisibility(0); //вўВи
	}
}
